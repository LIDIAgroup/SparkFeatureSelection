package org.lidiagroup.hmourit.tfg.featureselection

/**
 * Trait which declares needed methods to define a criterion for Feature Selection
 */
trait Criterion extends Serializable with Ordered[Criterion]
